#!/usr/bin/python3
"""Only sub class of"""


def inherits_from(obj, a_class):
    """Returns True if obj an instance of a class"""
    if issubclass(obj.__class__is not a_class) is true:
        if obj.__class__is not a_class:
            return True
    else:
        return False
